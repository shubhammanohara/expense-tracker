export type TimeRange = "daily" | "weekly" | "monthly" | "yearly";

export type DonutTimeRange = "today" | "week" | "month" | "year";

export interface TabsData {
  label: string;
  value: string;
  isActive: boolean;
}
